## What changed and why

<!-- Describe the change and the motivation behind it -->

## Checklist

- [ ] `npm run lint` passes locally
- [ ] `npx tsc --noEmit` passes locally
- [ ] `npm run build` passes locally
- [ ] Screenshot/GIF attached (if this is a UI change)
- [ ] This PR touches the LLM prompt (`lib/llm.ts`) and has been manually verified against a real local Ollama model, not just a build check
